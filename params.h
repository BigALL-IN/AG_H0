#pragma once

struct params {
    double a = 0; //lower bound
    double b = 0; //upper bound
    int p = 5; // precision
    int d = 2; //number of dimensions
    int it = 10000; //number of itterations
};